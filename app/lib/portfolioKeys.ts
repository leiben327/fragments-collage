/** localStorage JSON array of portfolio entries (image as data URL + meta). */
export const PORTFOLIO_STORAGE_KEY = "soft-pages:my-portfolio";

/** Fired on window after portfolio is saved, edited, or cleared. */
export const PORTFOLIO_UPDATED_EVENT = "soft-pages:portfolio-updated";
